{
  "name": "readit2",
  "version": "1.0.0",
  "private": true,
  "description": "Root package.json for monorepo - Railway should use readit2-backend directory",
  "scripts": {
    "build": "cd readit2-frontend && npm install && npm run build"
  }
}

